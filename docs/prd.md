# 可落地京东式商城系统（PC端专属·纯本地全容器化版）
## 业务架构与技术选型（Dubbo 3.x 纯开源栈·全K8s部署·开发环境优化）

## 一、核心定位与目标
- **用户规模**：支撑100-1000万注册用户，日活10-100万
- **并发能力**：峰值QPS 5000-20000，订单峰值1000-5000单/秒
- **架构目标**：服务化拆分完成，全容器化部署，具备完善的服务治理能力，支撑业务快速迭代
- **部署要求**：**100%本地部署+全容器化**，不依赖任何公有云服务，开发环境使用Docker Compose，生产环境使用K8s
- **运维理念**：开发阶段简单高效，生产阶段稳定可靠，环境一致性
- **终端范围**：仅PC端Web应用（含用户前台、商家后台、运营后台）

## 二、业务架构（DDD领域驱动设计）
### 2.1 核心业务域与微服务拆分（第三阶段标准）
采用**限界上下文**原则进行服务拆分，每个微服务对应一个独立的业务能力单元，拥有独立的数据库和缓存资源，开发阶段本地启动，生产阶段以K8s Pod形式运行。

| 业务域 | 微服务模块 | 核心职责 | 端口 |
|--------|-----------|----------|------|
| **用户域** | service-user、service-auth、service-member、service-address | 用户注册登录、身份认证、会员等级体系、收货地址管理 | 9301-9304 |
| **商品域** | service-product、service-category、service-inventory、service-review | 商品全生命周期管理、分类体系、库存控制、商品评价管理 | 9311-9314 |
| **交易域** | service-cart、service-order | 购物车管理、订单创建与状态流转、优惠计算、售后退款（内嵌于 order/payment） | 9321-9322 |
| **支付域** | service-payment | 支付单管理、退款处理（结算/对账为后期规划功能） | 9331 |
| **营销域** | service-coupon、service-seckill | 优惠券发放核销、限时秒杀（积分体系内嵌于 member） | 9351-9352 |
| **搜索域** | service-search | 商品全文检索、过滤排序、索引同步（Canal 监听 binlog 同步至 ES） | 9361 |
| **商家域** | service-shop | 商家入驻审核、店铺装修与管理、商家对账与结算 | 9341 |
| **平台域** | service-platform | 平台运营配置、商家/商品审核、风控规则 | 9381 |
| **基础支撑域** | service-file、service-notify | 文件存储与分发（MinIO）、短信邮件通知推送 | 9371-9372 |

### 2.2 第三阶段核心业务流程
1. **商品上架流程**：商家创建商品 → 平台审核 → 商品服务持久化 → 库存服务初始化 → 发送事务消息 → 索引同步服务更新搜索引擎
2. **用户下单流程**：浏览商品 → 加入购物车 → 提交订单 → 库存服务扣减库存 → 订单服务创建订单 → 支付服务发起支付 → 支付回调 → 订单状态更新 → 通知商家发货
3. **售后退款流程**：用户申请退款 → 商家审核 → 退货物流确认 → 商家确认收货 → 退款服务发起退款 → 支付回调 → 订单状态更新 → 库存服务回补库存

## 三、技术架构分层（Dubbo 3.x 专属·双环境兼容）
采用**七层分层架构**，开发环境和生产环境技术栈完全一致，仅部署方式不同。

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                                │
│  PC用户前台、商家后台、运营后台（Vue3 + Element Plus）        │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      网关层（统一入口）                       │
│  Spring Cloud Gateway（端口 8999）                            │
│  统一认证鉴权、限流熔断、请求路由、链路追踪                    │
│  （负责HTTP转Dubbo协议、请求路由、响应聚合、协议转换）        │
│  生产环境: 前置 MetalLB + Nginx Ingress + WAF（K8s）         │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      业务服务层（18 个微服务）                 │
│  Dubbo 3.2.13 微服务集群（用户、商品、订单、支付、营销等）     │
│  开发：本地IDE启动；生产：K8s Deployment                     │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      中间件层                                │
│  消息队列、分布式缓存、搜索引擎、分布式事务、分布式锁        │
│  开发：Docker Compose；生产：K8s StatefulSet                 │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                              │
│  关系型数据库（分库分表）、NoSQL数据库、本地对象存储、数据仓库│
│  开发：Docker Compose；生产：K8s StatefulSet+PV/PVC          │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      基础设施层                              │
│  开发：Docker Compose；生产：本地K8s集群+GitLab CI/CD        │
└─────────────────────────────────────────────────────────────┘
```

## 四、详细技术选型（100%本地开源·双环境兼容版）
### 4.1 前端技术栈（仅PC端）
| 技术领域 | 选型 | 版本 | 说明 |
|----------|------|------|------|
| **核心框架** | Vue 3 + Vite | 3.4.x / 5.0.x | 开发效率高，构建速度快 |
| **状态管理** | Pinia | 2.1.x | Vue 3官方推荐，比Vuex更简洁 |
| **HTTP客户端** | Axios | 1.6.x | 功能强大，支持请求/响应拦截器 |
| **UI组件库** | Element Plus | 2.6.x | 成熟稳定，组件丰富，适合后台系统 |
| **路由管理** | Vue Router | 4.3.x | 官方路由，支持嵌套路由和路由守卫 |
| **构建工具** | Vite | 5.0.x | 比Webpack快10倍以上的热更新和构建速度 |
| **代码规范** | ESLint + Prettier | 最新稳定版 | 统一团队代码风格 |

### 4.2 后端技术栈（核心：Java + Dubbo 3.x）
| 技术领域 | 选型 | 版本 | 说明 |
|----------|------|------|------|
| **编程语言** | Java | JDK 21 | 性能优异，生态完善，支持虚拟线程 |
| **开发框架** | Spring Boot | 3.2.5 | 简化配置，快速开发，与Dubbo深度集成 |
| **微服务框架** | Apache Dubbo | 3.2.13 | 高性能RPC框架，Triple 协议，阿里开源 |
| **服务注册发现** | Nacos | 2.3.x | 与Dubbo原生集成，同时支持配置中心 |
| **配置中心** | Nacos | 2.3.x | 动态配置热更新，支持灰度发布和配置隔离 |
| **API网关** | Spring Cloud Gateway | 4.1.x | 非阻塞异步架构，支持Dubbo协议转换 |
| **熔断降级** | Sentinel | 1.8.x | 与Dubbo、Nacos无缝集成，流量控制能力强 |
| **分布式事务** | Seata | 1.7.x | 支持AT、TCC、SAGA多种模式，与Dubbo集成 |
| **分布式锁** | Redisson | 3.25.x | 基于Redis的分布式锁实现，功能完善 |
| **ORM框架** | MyBatis-Plus | 3.5.x | 增强版MyBatis，大幅提高开发效率 |

### 4.3 中间件技术栈（双环境部署）
| 技术领域 | 选型 | 版本 | 开发部署 | 生产部署 |
|----------|------|------|----------|----------|
| **消息队列** | Apache RocketMQ | 5.3.x | Docker Compose单节点 | K8s StatefulSet 3节点集群 |
| **分布式缓存** | Redis | 7.2.x | Docker Compose单节点 | K8s StatefulSet 3主3从集群 |
| **搜索引擎** | Elasticsearch | 7.17.x | Docker Compose单节点 | K8s StatefulSet 3节点集群 |
| **分库分表** | ShardingSphere-JDBC | 5.4.x | 本地依赖 | 本地依赖 |
| **分布式任务调度** | XXL-Job | 2.4.x | Docker Compose单节点 | K8s Deployment 3节点集群 |
| **链路追踪** | Apache SkyWalking | 9.7.x | Docker Compose精简版 | K8s StatefulSet 3节点集群 |
| **日志收集** | ELK Stack | 7.17.x | 开发阶段可选 | K8s StatefulSet 3节点集群 |
| **数据同步** | Canal | 1.1.x | Docker Compose单节点 | K8s Deployment 3节点集群 |

### 4.4 数据存储技术栈（双环境部署）
| 技术领域 | 选型 | 版本 | 开发部署 | 生产部署 |
|----------|------|------|----------|----------|
| **关系型数据库** | MySQL | 8.0.x | Docker Compose单节点 | K8s StatefulSet 8主16从集群 |
| **NoSQL数据库** | MongoDB | 7.0.x | Docker Compose单节点 | K8s StatefulSet 3节点集群 |
| **本地对象存储** | MinIO | RELEASE.2024-05-10T01-41-38Z | Docker Compose单节点 | K8s StatefulSet 4节点集群 |
| **数据仓库** | ClickHouse | 23.8.x | 开发阶段可选 | K8s StatefulSet 3节点集群 |
| **本地镜像仓库** | Harbor | 2.10.x | 开发阶段可选 | K8s StatefulSet 3节点集群 |

### 4.5 基础设施与运维技术栈（双环境部署）
| 技术领域 | 选型 | 开发部署 | 生产部署 |
|----------|------|----------|----------|
| **容器运行时** | Docker | Docker Desktop | containerd |
| **容器编排** | Docker Compose / Kubernetes | Docker Compose | Kubernetes 1.29.x |
| **代码托管** | GitLab CE | 本地GitLab或GitHub | 本地K8s部署GitLab 3节点集群 |
| **CI/CD平台** | GitLab CI + GitLab Runner | 开发阶段可选 | K8s部署GitLab Runner动态伸缩 |
| **监控告警** | Prometheus + Grafana | 开发阶段可选 | K8s部署Prometheus 3节点集群 |
| **服务治理** | Dubbo Admin | Docker Compose单节点 | K8s Deployment 1节点 |
| **安全防护** | ModSecurity | 开发阶段可选 | Nginx Ingress集成 |
| **负载均衡** | Nginx | Docker Compose | MetalLB + Nginx Ingress |

## 五、开发环境Docker Compose部署方案

### 5.1 核心优势
- **一键启动**：一条命令启动所有中间件（13 个容器服务），5分钟内完成开发环境搭建
- **资源占用少**：单节点部署，适合开发机配置
- **环境一致**：所有开发者使用相同的中间件版本和配置
- **快速重置**：数据保存在本地卷，可随时清理和重置
- **平滑过渡**：技术栈与生产环境完全一致，部署时无需修改代码

### 5.2 中间件清单（19 个容器服务）

| 服务 | 镜像版本 | 默认主机端口 | 用途 |
|------|---------|------------|------|
| MySQL 8.0 | mysql:8.0.35 | 3306 | 关系型数据库（11 个数据库） |
| Redis 7.2 | redis:7.2-alpine | 6379 | 分布式缓存（256MB，LRU 淘汰，AOF） |
| Nacos 2.3 | nacos/nacos-server:v2.3.1 | 8848 / 9848 | 服务注册发现 + 配置中心（鉴权开启） |
| RocketMQ NameServer | apache/rocketmq:5.3.2 | 9876 | 消息队列 NameServer |
| RocketMQ Broker | apache/rocketmq:5.3.2 | 10911 / 10909 | 消息队列 Broker |
| RocketMQ Proxy | apache/rocketmq:5.3.2 | 8080 / 8081 | gRPC 代理 |
| RocketMQ Dashboard | apacherocketmq/rocketmq-dashboard:2.1.0 | 8082 | 消息队列可视化管理 |
| Elasticsearch（3 节点） | elasticsearch:7.17.23 | 9200 / 9201 / 9202 | 搜索引擎集群（IP 10.10.5.21/22/23） |
| Kibana | docker.elastic.co/kibana/kibana:7.17.23 | 5601 | ES 可视化管理 |
| MinIO | quay.io/minio/minio | 9000 / 9001 | 对象存储（API + Console） |
| XXL-Job 2.4 | xuxueli/xxl-job-admin:2.4.1 | 8083 | 分布式任务调度 |
| Sentinel 1.8 | bladex/sentinel-dashboard:1.8.0 | 8858 | 熔断降级控制台（sentinel/sentinel123） |
| SkyWalking OAP 9.7 | apache/skywalking-oap-server:9.7.0 | 11800 / 12800 | 链路追踪 OAP（存储后端 ES） |
| SkyWalking UI 9.7 | apache/skywalking-ui:9.7.0 | 8084 | 链路追踪 UI 控制台 |
| Seata 1.7 | seataio/seata-server:1.7.1 | 8091 / 7091 | 分布式事务（AT 模式，DB 存储） |
| Canal 1.1 | canal/canal-server:v1.1.7 | 11111 | MySQL binlog 数据同步（Admin 按需启用） |
| Prometheus | prom/prometheus:v2.51.2 | 9090 | 指标采集（微服务 + 中间件） |
| Grafana | grafana/grafana:10.4.2 | 3000 | 监控仪表盘（admin/admin123） |

> 所有端口通过 `.env` 环境变量配置（`${VAR:default}`），默认值使用中高位段避免常用端口冲突。ES 3 节点集群使用固定 IP 以解决 SkyWalking OAP Netty DNS 解析超时问题。开发环境 Nacos 鉴权默认开启，与生产安全策略保持一致。

### 5.3 数据库初始化（垂直分库）

```
业务库: db_user | db_product | db_order | db_payment | db_marketing | db_shop | db_platform
基础库: xxl_job | seata | nacos | canal_manager
```

启动时 MySQL 自动执行 `middleware-docker/mysql/init/` 目录下 10 个 SQL 脚本，完成建库建表。

## 六、GitLab CI/CD 全K8s部署方案（规划中）

> 当前阶段：CI/CD 流水线尚未搭建。开发阶段通过 Maven 命令行 + 运维脚本完成构建和启动。

### 6.1 核心架构原理（规划）
采用**"全本地+K8s原生"**的CI/CD架构，实现从代码提交到服务部署的全自动化：
- GitLab和GitLab Runner全部运行在本地K8s集群上
- GitLab Runner使用**K8s执行器**，每次构建自动创建临时Pod，构建完成后自动销毁
- 构建产物（Docker镜像）推送到本地Harbor镜像仓库
- 通过K8s API直接更新Deployment，实现滚动发布
- 整个过程完全在本地K8s集群内完成，无任何外部依赖

### 6.2 标准CI/CD流水线（规划）
每个微服务使用统一的`.gitlab-ci.yml`模板，包含4个阶段：
1. **代码检查**：执行CheckStyle代码检查，确保代码质量
2. **构建测试**：编译代码，执行单元测试，生成构建产物
3. **镜像打包**：构建Docker镜像，推送到本地Harbor仓库
4. **部署**：更新K8s Deployment，实现滚动发布

## 七、第三阶段核心技术落地原则
### 7.1 服务拆分原则
- 优先拆分**用户、商品、订单、支付**四大核心服务
- 每个服务独立部署，拥有独立的数据库和缓存资源
- 服务间通过Dubbo RPC调用，避免跨服务直接访问数据库
- 非核心服务（如评价、通知）可在核心服务稳定后逐步拆分

### 7.2 数据架构原则
- 按业务域进行**垂直分库**，每个业务域对应一个独立数据库
- 对大表（订单、商品）进行**水平分表**，采用哈希分片策略
- 实施**读写分离**，一主多从架构，主库负责写，从库负责读
- 冷热数据分离，历史数据归档到单独的历史库

### 7.3 高可用原则
- 所有中间件和服务均采用**集群部署**，避免单点故障
- 核心服务（订单、支付）独立部署，与非核心服务隔离
- 配置完善的**限流、熔断、降级**规则，防止系统雪崩
- 建立完善的监控告警体系，实现故障快速发现和定位

### 7.4 性能优化原则
- 构建**多级缓存体系**（本地缓存Caffeine + Redis分布式缓存）
- 所有非实时操作全部**异步化**处理，通过消息队列解耦
- 对热点数据进行预加载和缓存预热
- 优化数据库索引和SQL语句，避免慢查询

### 7.5 部署原则
- **开发环境**：使用Docker Compose部署中间件，业务服务本地IDE启动
- **测试环境**：使用Docker Compose或轻量K8s集群部署所有服务
- **生产环境**：使用完整K8s集群部署，StatefulSet部署有状态服务，Deployment部署无状态服务
- 所有配置使用**环境变量**或配置中心管理，不硬编码在代码中

## 八、部署架构（双环境版）
### 8.1 开发环境部署架构（当前阶段）
```
┌─────────────────────────────────────────────────────────────┐
│                      开发者本地机器                          │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 前端 (Vite dev server)                               │    │
│  │ app-b2c:5173 / app-b2b:5174 / app-admin:5175        │    │
│  └────────────┬────────────────────────────────────────┘    │
│               │ HTTP (Axios)                                 │
│               ▼                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Spring Cloud Gateway (localhost:8999)                 │    │
│  │ JWT 鉴权 → 路由转发 → 透传 X-User-Id / X-User-Roles  │    │
│  └────────────┬────────────────────────────────────────┘    │
│               │ Dubbo 3.2.13 Triple Protocol                 │
│               ▼                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 18 个 Dubbo 微服务 (IDE 直接启动，端口 9301-9381)     │    │
│  │ service-user / service-auth / service-order / ...    │    │
│  └────────────┬────────────────────────────────────────┘    │
│               │                                              │
│               ▼                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Docker Compose 中间件 (19 个容器)                     │    │
│  │ MySQL / Redis / Nacos / RocketMQ / ES×3 / MinIO /   │    │
│  │ Sentinel / SkyWalking / Seata / Canal / Prometheus / │    │
│  │ Grafana / Kibana / XXL-Job                            │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 生产环境部署架构（规划中）
```
┌─────────────────────────────────────────────────────────────┐
│                      本地K8s集群入口                        │
│  本地DNS → MetalLB(四层LB) → Nginx Ingress → ModSecurity WAF │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      业务应用层                             │
│  Spring Cloud Gateway(3节点)、Dubbo微服务集群(各3-5节点)     │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      中间件集群                              │
│  Redis Cluster(3主3从)、RocketMQ(3节点)、Elasticsearch(3节点)│
│  Nacos(3节点)、Sentinel(3节点)、SkyWalking(3节点)           │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      数据存储集群                            │
│  MySQL(8主16从)、MongoDB(3节点)、MinIO(4节点)、ClickHouse(3节点)│
│  Harbor本地镜像仓库(3节点)                                  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      基础设施层                              │
│  GitLab(3节点)、GitLab Runner(动态伸缩)、Prometheus(3节点)   │
│  Grafana(1节点)、Kibana(1节点)、Dubbo Admin(1节点)          │
└─────────────────────────────────────────────────────────────┘
```

## 九、当前进展与实施建议

### 9.1 当前阶段（2026-05）

- **Phase 1（基础设施搭建）**：✅ 已完成 — Docker Compose 19 个容器全部就绪，Maven 父工程 + 5 个公共模块完成，18 个微服务脚手架生成，Gateway 配置完成，运维脚本就绪
- **Phase 2+**：🔄 进行中 — 业务功能按服务逐个实现
- **配置体系**：每个服务使用 `bootstrap.yml`（服务发现 + Nacos 配置中心）+ `application-dev.yml`（数据源、中间件）+ `.env` 环境变量，无裸 `application.yml`
- **前端**：3 个前端应用（app-b2c:5173 / app-b2b:5174 / app-admin:5175），pnpm Monorepo 架构

### 9.2 实施建议
1. **先搭开发环境**：✅ 已完成 — 使用 `bash scripts/start-gateway.sh` 和 `bash scripts/start-service.sh <name>` 启动服务
2. **统一开发规范**：配置规范已建立（bootstrap + application-dev + .env），代码规范通过 CheckStyle 检查
3. **分阶段开发**：先开发核心服务（用户、商品、订单、支付），再开发营销、搜索等扩展服务
4. **同步建设测试环境**：每个服务已有 `src/test/resources/application-test.yml`，单元测试通过 `mvn -pl <module> test` 运行
5. **逐步迁移到生产**：K8s 部署配置已编写（`super-market-k8s/`），待功能稳定后迁移
6. **自动化运维**：CI/CD 为规划阶段，当前使用 `scripts/` 目录下的 Shell 脚本管理服务启停
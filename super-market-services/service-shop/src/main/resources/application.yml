spring:
  application:
    name: shop-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9341}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

springdoc:
  api-docs:
    path: /api/shop/v3/api-docs
  group-configs:
    - group: service-shop
      paths-to-match: /api/shop/**

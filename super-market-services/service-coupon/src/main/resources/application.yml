spring:
  application:
    name: coupon-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9351}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: inventory-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9313}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

springdoc:
  api-docs:
    path: /api/inventory/v3/api-docs
  group-configs:
    - group: service-inventory
      paths-to-match: /api/inventory/**

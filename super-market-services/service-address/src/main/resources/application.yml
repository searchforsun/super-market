spring:
  application:
    name: address-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9304}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

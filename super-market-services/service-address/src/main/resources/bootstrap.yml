spring:
  config:
    import: optional:nacos:localhost:8848
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yaml
        enabled: false
      discovery:
        server-addr: localhost:8848
        namespace: public
        enabled: false

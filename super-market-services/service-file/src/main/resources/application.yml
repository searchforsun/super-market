spring:
  application:
    name: file-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: ${SERVER_PORT:9071}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

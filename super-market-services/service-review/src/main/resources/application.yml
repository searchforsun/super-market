spring:
  application:
    name: review-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:${DOCKER_HOST_IP:localhost}:${NACOS_PORT:8848}"

server:
  port: ${SERVER_PORT:9314}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

springdoc:
  api-docs:
    path: /api/review/v3/api-docs
  group-configs:
    - group: service-review
      paths-to-match: /api/review/**

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

spring:
  application:
    name: review-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: ${SERVER_PORT:9314}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

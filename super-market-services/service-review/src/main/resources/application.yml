spring:
  application:
    name: review-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: ${SERVER_PORT:9014}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

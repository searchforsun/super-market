spring:
  application:
    name: platform-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: ${SERVER_PORT:9081}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

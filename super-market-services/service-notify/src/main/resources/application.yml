spring:
  application:
    name: notify-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9372}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

springdoc:
  api-docs:
    path: /api/notify/v3/api-docs
  group-configs:
    - group: service-notify
      paths-to-match: /api/notify/**

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

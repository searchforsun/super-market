spring:
  application:
    name: auth-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: ${SERVER_PORT:9002}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

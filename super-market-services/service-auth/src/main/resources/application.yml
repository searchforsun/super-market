spring:
  config:
    import: optional:nacos:localhost:8848
  application:
    name: auth-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9302}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

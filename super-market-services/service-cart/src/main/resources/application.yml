spring:
  application:
    name: cart-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9321}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

springdoc:
  api-docs:
    path: /api/cart/v3/api-docs
  group-configs:
    - group: service-cart
      paths-to-match: /api/cart/**

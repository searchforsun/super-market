spring:
  application:
    name: order-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9322}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

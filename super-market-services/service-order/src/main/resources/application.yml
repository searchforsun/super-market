spring:
  application:
    name: order-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: ${SERVER_PORT:9022}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

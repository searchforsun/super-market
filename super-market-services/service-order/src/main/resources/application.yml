spring:
  application:
    name: order-service
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  config:
    import: "optional:nacos:localhost:8848"

server:
  port: ${SERVER_PORT:9322}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

springdoc:
  api-docs:
    path: /api/order/v3/api-docs
  group-configs:
    - group: service-order
      paths-to-match: /api/order/**

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: super-market
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  ports:
    - name: dubbo
      port: 9321
      targetPort: 9321
      protocol: TCP
  type: ClusterIP

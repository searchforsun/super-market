apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: super-market
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - name: dubbo
      port: 9322
      targetPort: 9322
      protocol: TCP
  type: ClusterIP

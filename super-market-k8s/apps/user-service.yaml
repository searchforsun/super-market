apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: super-market
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - name: dubbo
      port: 9301
      targetPort: 9301
      protocol: TCP
  type: ClusterIP
